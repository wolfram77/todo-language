default datatypes:
integer, number, char, string

a, b, c & d = 0 @integer;
integer a, b, c & d = 0;

making a custom datatype:
school cstring := {
	string a;
	
};

"To find the winne+r: Alice vs Bob"

t = in.number
for 1 to t {
	n = in.number
	if n % 2 == 0 then out 'ALICE'
				  else out 'BOB'
}
